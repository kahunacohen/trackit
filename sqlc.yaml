version: "2"
sql:
  - engine: sqlite
    queries:   
      - internal/db/queries

    schema: internal/db/migrations
    gen:
      go:
        package: models
        out: internal/models
                